/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["lh3.googleusercontent.com","photos.google.com", "github.com", "img.shields.io"]
    },
}

module.exports = nextConfig
